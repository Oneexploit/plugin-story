You are a senior WordPress plugin architect, security reviewer, QA engineer, and Elementor compatibility expert.

I have a WordPress plugin called **Prime Stories**.

Your task is to perform a complete professional review, debugging, optimization, and hardening pass on the entire plugin codebase.

Do not just give general advice. Inspect the actual files, identify problems, fix them, and make the plugin production-ready.

---

# Main Goal

Review the full plugin and make sure it:

* Installs without fatal errors
* Activates without fatal errors
* Works without Elementor installed
* Works correctly when Elementor is installed
* Works without WooCommerce installed
* Works correctly when WooCommerce is installed
* Works on PHP 8.0+
* Works on the latest stable WordPress version
* Follows WordPress coding standards
* Is secure
* Is optimized
* Is responsive
* Is RTL-compatible
* Does not break the frontend
* Does not break the WordPress admin
* Can be zipped and installed as a normal WordPress plugin

---

# 1. Full File Structure Check

First, inspect the plugin folder and verify that all required files exist.

Expected structure:

```text
prime-stories/
├── prime-stories.php
├── uninstall.php
├── includes/
├── admin/
├── public/
├── elementor/
└── assets/
```

Check:

* Missing files
* Wrong file names
* Wrong class names
* Wrong include paths
* Broken require/include statements
* Incorrect plugin constants
* Incorrect folder URLs
* Direct access protection in every PHP file

Every PHP file must have:

```php
defined('ABSPATH') || exit;
```

except `uninstall.php`, which should have proper uninstall protection.

---

# 2. Activation / Deactivation Check

Review plugin activation and deactivation logic.

Check:

* Activation hook works
* Deactivation hook works
* No fatal errors during activation
* Custom database table is created correctly
* Rewrite rules are flushed only when necessary
* Default options are created correctly
* No unnecessary frontend output during activation
* No dependency fatal errors if Elementor/WooCommerce are missing

Fix any issue found.

---

# 3. WordPress Standards Check

Review the plugin for WordPress best practices.

Check:

* Correct use of hooks
* Correct use of actions and filters
* Correct Custom Post Type registration
* Correct taxonomy registration
* Correct Settings API usage
* Correct admin menu registration
* Correct script/style enqueueing
* Correct shortcode registration
* Correct REST API/AJAX registration
* Correct database table creation using `dbDelta`
* Correct use of WordPress functions instead of raw PHP where appropriate

Fix all violations.

---

# 4. Security Audit

Perform a complete security audit.

Check and fix:

* Missing nonce checks
* Missing capability checks
* Unsanitized input
* Unescaped output
* Unsafe SQL queries
* Missing `$wpdb->prepare()`
* Unsafe AJAX/REST endpoints
* Unsafe file/media handling
* Unsafe URL handling
* Unsafe HTML output
* Unsafe JSON output
* Missing permission callbacks in REST API routes
* Missing validation of analytics event types
* Missing validation of story IDs
* Possible XSS issues
* Possible CSRF issues
* Possible privilege escalation issues

Use appropriate functions:

```php
sanitize_text_field()
sanitize_textarea_field()
sanitize_key()
sanitize_email()
esc_html()
esc_attr()
esc_url()
wp_kses_post()
absint()
wp_verify_nonce()
current_user_can()
check_admin_referer()
check_ajax_referer()
$wpdb->prepare()
```

Do not allow any unsafe request data to be saved or rendered.

---

# 5. PHP Error Check

Search for and fix:

* Syntax errors
* Undefined variables
* Undefined indexes
* Undefined constants
* Missing classes
* Wrong namespaces
* Wrong method names
* Wrong hook callbacks
* Static/non-static method misuse
* Incorrect constructor usage
* Fatal errors on plugin activation
* Fatal errors on frontend
* Fatal errors in admin
* PHP 8 warnings
* Deprecated PHP usage

Make the code clean under:

```text
WP_DEBUG = true
WP_DEBUG_LOG = true
WP_DEBUG_DISPLAY = false
```

The plugin should not generate warnings or notices during normal usage.

---

# 6. Elementor Compatibility Review

Check the Elementor integration carefully.

Requirements:

* Plugin must not crash if Elementor is not installed
* Plugin must not crash if Elementor is inactive
* Elementor widget should only register when Elementor is available
* Widget class should be loaded safely
* Widget controls should be registered correctly
* Widget should render correctly in Elementor editor
* Widget should render correctly on frontend
* Elementor preview mode should work
* Widget should reuse the same frontend rendering system as shortcode
* No duplicate rendering logic
* No dependency on Elementor for core plugin functionality

Fix any issue.

---

# 7. WooCommerce Compatibility Review

Check WooCommerce-related display rules.

Requirements:

* Plugin must not crash if WooCommerce is not installed
* WooCommerce conditions should only run if WooCommerce exists
* Product and product category display rules should fail safely
* No hard dependency on WooCommerce
* No fatal errors on non-WooCommerce websites

Fix any issue.

---

# 8. Shortcode Review

Review the shortcode:

```text
[prime_stories]
```

Check:

* Shortcode works on pages
* Shortcode works on posts
* Shortcode works inside Elementor shortcode widget
* Shortcode attributes are sanitized
* Invalid attributes fail safely
* Empty story groups do not break layout
* Assets load when shortcode exists
* Multiple shortcodes on the same page work correctly
* Multiple story groups on the same page do not conflict
* Unique wrapper IDs are generated when needed

Fix any issue.

---

# 9. Display Rules Review

Review automatic display rules.

Check:

* Homepage condition works
* Entire website condition works
* Specific pages condition works
* Specific posts condition works
* Category condition works
* Blog archive condition works
* Search page condition works
* 404 condition works
* WooCommerce product condition works safely
* WooCommerce product category condition works safely
* Device condition works
* User condition works
* Floating positions work
* Before/after content positions work
* Rules can be enabled/disabled
* Invalid rules do not break frontend
* Empty story groups do not render broken HTML

Fix all problems.

---

# 10. Story Query Logic Review

Review story fetching logic.

Check:

* Only active/scheduled stories are displayed
* Expired stories are hidden
* Inactive stories are hidden
* Start datetime works
* End datetime works
* Device targeting works
* User targeting works
* Priority/order works
* Limit works
* Group filtering works
* Missing media does not break frontend
* Draft/private stories are not shown publicly
* Query is efficient
* No unnecessary heavy queries

Fix any issue.

---

# 11. Frontend Viewer QA

Test and review the frontend story viewer.

Requirements:

* Opens correctly
* Closes correctly
* Next works
* Previous works
* Progress bar works
* Image stories work
* Video stories work
* CTA button works
* CTA click tracking works
* Captions display correctly
* Keyboard controls work
* Escape closes viewer
* Arrow keys navigate
* Swipe works on mobile
* Pause on hold works
* Seen/unseen state works
* Multiple story bars on same page work
* Viewer does not create duplicate overlays
* Viewer does not conflict with themes
* Viewer does not create console errors
* Viewer is responsive
* Viewer works in RTL websites

Fix all JavaScript and CSS issues.

---

# 12. Analytics Review

Review analytics implementation.

Check:

* Analytics table is created correctly
* Impressions are tracked
* Opens are tracked
* Completions are tracked
* CTA clicks are tracked
* Invalid event types are rejected
* Invalid story IDs are rejected
* User ID is stored only when user is logged in
* Session ID is handled safely
* SQL queries are prepared
* Admin analytics page loads correctly
* CTR calculation is correct
* Completion rate calculation is correct
* Analytics can be disabled from settings
* Disabled analytics does not record events

Fix all issues.

---

# 13. Admin UI Review

Review the admin experience.

Check:

* Stories menu appears correctly
* Add Story screen works
* Meta boxes display correctly
* Media uploader works
* Fields save correctly
* Fields load correctly after save
* Story Groups work
* Settings page works
* Display Rules page works
* Analytics page works
* Admin notices are clear
* No broken styling
* No broken JavaScript
* Admin assets load only where needed
* All labels are clear and professional

Fix all issues.

---

# 14. Settings Review

Review settings.

Check:

* Settings save correctly
* Settings sanitize correctly
* Defaults work
* Missing options do not break plugin
* Style settings are applied correctly
* Custom CSS is output safely
* Performance settings work
* Analytics enable/disable works
* Seen state enable/disable works
* Remove data on uninstall option works correctly

Fix all issues.

---

# 15. Asset Loading Review

Check frontend and admin asset loading.

Requirements:

* Frontend CSS/JS should not load globally unless needed or setting is enabled
* Assets load when shortcode is used
* Assets load when Elementor widget is used
* Assets load when display rule applies
* Admin assets load only on plugin admin pages and story edit screens
* No duplicate asset loading
* CSS and JS versions use plugin version
* No missing asset URLs
* No 404 asset requests

Fix any issue.

---

# 16. CSS Review

Review CSS.

Check:

* All frontend classes are prefixed with `prime-stories-`
* Styles are scoped
* No theme-breaking global styles
* Responsive design works
* Mobile layout works
* Desktop layout works
* RTL layout works
* Floating layout works
* Viewer overlay z-index is high enough but reasonable
* No layout shift issues
* No inaccessible color contrast where obvious

Fix CSS problems.

---

# 17. JavaScript Review

Review JavaScript.

Check:

* No console errors
* No global pollution except `window.PrimeStories`
* Multiple instances work
* Event listeners are not duplicated unnecessarily
* Viewer state is handled correctly
* localStorage errors are handled safely
* Video autoplay restrictions are handled gracefully
* Lazy loading works
* AJAX/REST calls handle failures gracefully
* Tracking does not block UX
* Keyboard events do not interfere unnecessarily with the page

Fix JavaScript problems.

---

# 18. Internationalization / RTL Review

Check:

* All user-facing PHP strings use translation functions:

  * `__()`
  * `_e()`
  * `esc_html__()`
  * `esc_attr__()`

* Text domain is consistent:

  * `prime-stories`

* RTL frontend works

* RTL admin does not break

* CSS respects `is_rtl()` where needed

Fix all issues.

---

# 19. Database Review

Review database usage.

Check:

* Custom table name uses `$wpdb->prefix`
* Table creation uses `dbDelta`
* Charset/collation uses `$wpdb->get_charset_collate()`
* Queries are prepared
* Indexes are reasonable
* Analytics table does not grow unnecessarily without future extension points
* Uninstall cleanup is safe and optional

Fix any issue.

---

# 20. Performance Review

Review performance.

Check:

* No heavy queries on every page
* No loading all stories unnecessarily
* No loading all analytics data unnecessarily
* No frontend assets when not needed
* Lazy loading is used
* Media URLs are fetched efficiently
* Admin pages do not run expensive queries unnecessarily
* Display rules are evaluated efficiently
* Caching-friendly output where possible

Fix performance issues.

---

# 21. Accessibility Review

Improve accessibility where practical.

Check:

* Buttons have labels
* Close button has accessible label
* Next/previous buttons have accessible labels
* Images have alt attributes
* Viewer uses reasonable focus behavior
* Keyboard navigation works
* CTA buttons are proper links/buttons
* Color-only status indicators are avoided where possible

Fix accessibility issues.

---

# 22. Uninstall Review

Review `uninstall.php`.

Check:

* It only runs during uninstall
* It checks the plugin uninstall constant
* It respects “Remove plugin data on uninstall”
* It removes plugin options if enabled
* It removes analytics table if enabled
* It removes story meta if enabled
* It does not delete user content unless explicitly configured
* It does not create fatal errors

Fix any issue.

---

# 23. Final Testing Checklist

After fixing the code, run through this checklist and report the result.

## Installation

* Plugin zip installs successfully
* Plugin activates successfully
* Plugin deactivates successfully
* No fatal errors

## Admin

* Can create story group
* Can create image story
* Can create video story
* Can add caption
* Can add CTA
* Can set schedule
* Can save and reload fields
* Can configure settings
* Can create display rule
* Can view analytics page

## Frontend

* Shortcode renders stories
* Elementor widget renders stories
* Display rule renders stories
* Story viewer opens
* Story viewer closes
* Next/previous works
* CTA works
* Analytics records events
* Seen state works
* Mobile layout works
* Desktop layout works
* RTL works

## Compatibility

* Works without Elementor
* Works with Elementor
* Works without WooCommerce
* Works with WooCommerce
* Works with PHP 8.0+
* Works with WP_DEBUG enabled

---

# 24. Required Output

After reviewing and fixing the plugin, provide:

1. A summary of all issues found
2. A summary of all fixes applied
3. A list of files changed
4. Any remaining known limitations
5. Final installation instructions
6. Final usage instructions
7. Final shortcode examples
8. Final Elementor usage instructions
9. Final QA checklist result

Important:

Do not only explain what should be fixed. Actually fix the code.

If a file is missing, create it.

If a class is broken, correct it.

If a feature is incomplete, complete it.

If a feature cannot be completed safely, explain why and provide the safest working alternative.

The final result must be a professional, stable, secure, installable WordPress plugin.
